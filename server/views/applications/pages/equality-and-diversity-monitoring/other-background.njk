{% from "../../../components/formFields/form-page-input/macro.njk" import formPageInput %}

{% extends "../layout.njk" %}
{% block questions %}
  <span class="govuk-caption-l">{{ page.title }}</span>

  {% set optionalOtherBackground %}
    {{
      formPageInput(
        {
          label: { text: page.questions.optionalOtherBackground},
          classes: "govuk-input--width-20",
          fieldName: "optionalOtherBackground"
        },
        fetchContext()
      )
    }}
  {% endset -%}

  {{
    formPageRadios(
      {
        fieldName: "otherBackground",
        fieldset: {
          legend: {
            text: page.questions.otherBackground,
            isPageHeading: true,
            classes: "govuk-fieldset__legend--l"
          }
        },
        items: page.items(optionalOtherBackground)
      },
      fetchContext()
    )
  }}
{% endblock %}