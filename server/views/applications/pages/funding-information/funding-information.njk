{% extends "../layout.njk" %}
{% block questions %}
  <span class="govuk-caption-l">{{ page.title }}</span>
  <h1 class="govuk-heading-l">{{ page.questions.fundingSource }}</h1>

  {{
    formPageRadios(
      {
        fieldName: "fundingSource",
        items: page.items()
      },
      fetchContext()
    )
  }}
{% endblock %}
